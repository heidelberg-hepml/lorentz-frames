exp_name: sweep-top-tr-1

trials: 1  # one trial run per job
seed: 0
n_startup_trials: 400
optuna_db: 'runs/${exp_name}/optuna/optuna.db'

model_name: tag_transformer

cfg_overrides:
 - model.net.num_blocks=8
