exp_name: sweep-eg-tr-1

trials: 1  # one trial run per job
seed: 0
n_startup_trials: 100
optuna_db: 'runs/${exp_name}/optuna/optuna.db'

model_name: eg_transformer

cfg_overrides:
 - evaluation.classifier=false
 - evaluation.sample=false
 - evaluation.eval_log_prob=[val]
